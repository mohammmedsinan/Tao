/**
 * CRT Overlay Component
 * Adds a subtle scanline effect over the entire app for that retro CRT feel
 */
export default function CRTOverlay() {
    return (
        <div
            className="crt-overlay"
            aria-hidden="true"
        />
    )
}
